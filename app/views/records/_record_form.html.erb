<div class="well">

    <%= simple_form_for(@record, html: { class: 'form-horizontal' }) do |f| %>

    <p>
      <%= f.association :manager, label_method: :first_name, value_method: :id, include_blank: false %>
    </p>
    <p>
      <%= f.association :client, label_method: :first_name, value_method: :id, include_blank: false %>
    </p>
    <p>
      <%= f.input :start_date, as: :date, order: [:day, :month, :year] %>
    </p>
    <p>
      <%= f.input :steps_taken, as: :text, :input_html => {:rows => 5}  %>
    </p>
    <p>
      <%= f.input :notes, as: :text, :input_html => {:rows => 5}  %>
    </p>
    <p>
      <%= f.association :stage, include_blank: false%>
    </p>
    <h3>Activities</h3>

    <div id="activities">
      <%= f.simple_fields_for :activities do |activity| %>
        <%= render 'activity_fields', :f => activity %>
      <%end%>
      <div class="btn btn-primary" type="button">
        <%=link_to_add_association 'Add New Activity', f, :activities%>
      </div>
    </div>

    <div id= "buttonId" class="actions">
      <%= f.submit "Submit", class:"btn btn-primary btn-padded" %>
    </div>
  <% end %>
</div>
